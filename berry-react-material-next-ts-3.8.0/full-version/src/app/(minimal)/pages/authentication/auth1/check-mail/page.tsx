// PROJECT IMPORTS
import CheckMail from 'views/authentication/auth1/check-mail';

// ================================|| CHECK MAIL ||================================ //

const CheckMailPage = () => {
  return <CheckMail />;
};

export default CheckMailPage;
